<%= form_with(model: fan, local: true) do |form| %>
  <% if fan.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(fan.errors.count, "error") %> prohibited this fan from being saved:</h2>

      <ul>
        <% fan.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :enthusiasm_level %>
    <%= form.number_field :enthusiasm_level %>
  </div>

  <div class="field">
    <%= form.label :favorite_player %>
    <%= form.text_field :favorite_player %>
  </div>

  <div class="field">
    <%= form.label :email %>
    <%= form.text_field :email %>
  </div>

  <div class="field">
    <%= form.label :hooliganism_level %>
    <%= form.number_field :hooliganism_level %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
